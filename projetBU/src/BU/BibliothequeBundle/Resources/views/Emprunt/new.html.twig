{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'un emprunt</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}
   <h2>{{ message }}</h2>
    <ul>
        <li>
            <a href="{{ path('emprunt_index') }}">Retouner à la liste</a>
        </li>
    </ul>
{% endblock %}
